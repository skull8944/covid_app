class CovidInfo {

  String date;
  String totalConfirmed;
  String newConfirmed;
  String totalDeath;
  String newDeath;

  CovidInfo(this.date, this.totalConfirmed, this.newConfirmed, this.totalDeath, this.newDeath);
}